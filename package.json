{
  "name": "currency-codes",
  "version": "2.2.0",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "data"
  ],
  "description": "Lookup currency codes based on ISO 4217",
  "repository": "git://github.com/freeall/currency-codes.git",
  "keywords": [
    "iso4217",
    "iso",
    "4217",
    "currency",
    "currencies",
    "code",
    "codes",
    "lookup"
  ],
  "devDependencies": {
    "@gouch/to-title-case": "^2.2.1",
    "@types/node": "^20.0.0",
    "@vitest/ui": "^3.2.4",
    "axios": "^0.19.2",
    "fast-xml-parser": "^4.3.0",
    "tsup": "^8.0.0",
    "tsx": "^4.20.5",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build",
    "test": "npm run build && vitest run",
    "test:watch": "npm run build && vitest",
    "test:ui": "npm run build && vitest --ui",
    "test:performance": "npm run build && vitest run ingest.performance.test.ts",
    "test:esm": "npm run build && node --input-type=module --eval \"import('./dist/index.mjs').then(cc => { console.log('ESM import successful:', typeof cc.code); console.log('USD test:', cc.code('USD').currency); })\"",
    "test:cjs": "npm run build && node --eval \"const cc = require('./dist/index.js'); console.log('CommonJS require works:', typeof cc.code); console.log('USD test:', cc.code('USD').currency)\"",
    "iso:fetch-xml": "tsx scripts/iso-4217/fetch.ts",
    "iso:ingest-xml": "tsx scripts/iso-4217/ingest.ts",
    "iso": "npm run iso:fetch-xml && npm run iso:ingest-xml"
  },
  "license": "MIT"
}
